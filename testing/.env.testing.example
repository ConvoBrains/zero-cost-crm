# Copy to testing/.env.testing (gitignored). Used ONLY by make test-* targets.
# Never point these at production RDS.

DATABASE_URL=postgresql://crm_test:crm_test_local_only@127.0.0.1:5434/brains_crm_test
DB_URL_DEV=postgresql://crm_test:crm_test_local_only@127.0.0.1:5434/brains_crm_test
DB_SSL=false
ALLOW_ACTIVITY_SEED=1

PORT=4000
JWT_SECRET=testing-jwt-secret-not-for-prod

# Optional — recordings not required for Activity UI fixture testing
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1
AWS_S3_BUCKET=
